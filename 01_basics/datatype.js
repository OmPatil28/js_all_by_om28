"use srtict;" // treat all js code as new version

// alert(3 + 3) we are using nodejs not browser

// console.log(typeof "om");

// primitive data type 

// 7 type = string, numbeer, boolean, null, undefined, symbol, bigIN
// this are some dayatype in primitive 

const score = 100;
const scorevalue = 100.5;

const isloggedIn = true;
const outsidetemp = null;
const useremail = undefined;



// nonprimitive datatype 
// array, abjecgt, function,


const heros = ["shaktiman", "nagraj", "don"]

let obj = {
    name: "om",
    age: 21
};

const myFunction = function() {
    console.log("hello world");
}